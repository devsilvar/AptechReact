import React from 'react';
import { Link, Outlet } from 'react-router-dom';
import Footer from '../Footer';

const Root = () => {
  return (
    <section>
      <h1>I am the root</h1>
    </section>
  );
};

export default Root;
